SPECIFICATION Spec
CONSTANTS Nodes = {n1, n2, n3, n4}, MaxFailures = 2
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS SystemResilient
CONSTRAINT Cardinality(experiments) < 6

