SPECIFICATION Spec

CONSTANTS
    MinKeys = 2
    MaxKeys = 4
    TotalKeys = 10
    \* CORE constants (required since TTree EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS
    TypeOK_TTree
    BST_Property
    BoundingValues
    NodeCapacity

PROPERTIES
    SearchCorrectness

CONSTRAINT
    nodeCount <= 10

CHECK_DEADLOCK FALSE
