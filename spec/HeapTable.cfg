\* TLC Configuration for HeapTable.tla

SPECIFICATION Spec_Heap

CONSTANTS
  MAX_TX = 3
  MAX_LSN = 20
  MAX_PAGES = 5
  PAGE_SIZE = 1024

INVARIANTS
  TypeOK_Heap
  Inv_Heap_SlotConsistency
  Inv_Heap_FreeSpaceValid
  Inv_Heap_PageChecksum

CONSTRAINT
  Cardinality(allocatedRIDs) <= 20
