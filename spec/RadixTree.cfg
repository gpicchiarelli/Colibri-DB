SPECIFICATION Spec

CONSTANTS
    MaxNodes = 20
    MaxKeyLength = 8
    \* CORE constants (required since RadixTree EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 20
    PAGE_SIZE = 1024

INVARIANTS
    TypeOK_RadixTree
    PrefixCompression
    UniqueKeys

CONSTRAINT
    nodeCount <= 20

CHECK_DEADLOCK FALSE
