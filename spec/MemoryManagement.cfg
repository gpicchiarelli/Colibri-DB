SPECIFICATION Spec
CONSTANTS TotalMemory = 1000, MaxArenas = 10, BlockSizes = {8, 16, 32, 64, 128}
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS MemoryConsistent
CONSTRAINT Cardinality(arenas) < 8

