SPECIFICATION Spec
CONSTANTS Data = {1, 2, 3, 4, 5}, GroupKeys = {g1, g2, g3}
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS AggregateCorrect

