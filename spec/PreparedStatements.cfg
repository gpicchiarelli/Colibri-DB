SPECIFICATION Spec
CONSTANTS
    MaxStatements = 10
    Connections = {c1, c2}
    SQLTemplates = {SELECT, INSERT, UPDATE}

INVARIANTS
    TypeOK
    Safety
    StatementLimitEnforced

CHECK_DEADLOCK FALSE

