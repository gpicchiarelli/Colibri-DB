SPECIFICATION Spec
CONSTANTS MaxDatabases = 20, MaxTables = 50
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS UniqueDatabase
CONSTRAINT Cardinality(databases) < 10

