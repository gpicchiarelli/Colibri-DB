SPECIFICATION Spec
CONSTANTS Algorithms = {LZ4, SNAPPY, ZSTD, GZIP}, MaxBlocks = 50, CompressionLevels = {1, 3, 6, 9}
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS ValidCompressionRatio
CONSTRAINT Len(compressedBlocks) < 20

