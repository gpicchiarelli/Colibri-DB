SPECIFICATION Spec
CONSTANTS BaseTables = {t1, t2}, MaxViews = 10
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS ViewsConsistent
CONSTRAINT Cardinality(views) < 5

