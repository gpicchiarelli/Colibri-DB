SPECIFICATION Spec
CONSTANTS MaxFiles = 50, MaxSnapshots = 10, MaxClones = 20
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS ValidRefCounts
CONSTRAINT Cardinality(DOMAIN files) < 15

