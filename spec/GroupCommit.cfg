\* TLC Configuration for GroupCommit.tla

SPECIFICATION Spec_GC

CONSTANTS
  MAX_TX = 3
  MAX_LSN = 20
  MAX_PAGES = 5
  MAX_BATCH_SIZE = 5
  MAX_WAIT_TIME_MS = 10
  PAGE_SIZE = 1024
  GROUP_COMMIT_THRESHOLD = 3
  ModifiablePages = {1, 2, 3}

INVARIANTS
  TypeOK_GC
  Inv_GC_DurabilityPreserved
  Inv_GC_OrderPreserved
  Inv_GC_BoundedWait

PROPERTIES
  Liveness_EventualCommit

CONSTRAINT
  Len(commitQueue) <= MAX_BATCH_SIZE
