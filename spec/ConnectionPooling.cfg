SPECIFICATION Spec
CONSTANTS MaxConnections = 20, MaxIdleTime = 300
    * CORE constants (required since module EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS PoolSizeValid
CONSTRAINT currentTime < 100

