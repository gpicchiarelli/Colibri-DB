SPECIFICATION Spec

CONSTANTS
    MaxLevel = 4
    Probability = 2
    MaxKeys = 10
    \* CORE constants (required since SkipList EXTENDS CORE)
    MAX_TX = 3
    MAX_LSN = 20
    MAX_PAGES = 10
    PAGE_SIZE = 1024

INVARIANTS
    TypeOK_SkipList
    OrderedKeys
    LevelConsistency
    PointerValidity

PROPERTIES
    InsertEventuallyComplete
    SearchCorrectness

CONSTRAINT
    size <= 10

CHECK_DEADLOCK FALSE
