SPECIFICATION Spec_IndexSubsystem

CONSTANTS
  MAX_TX = 3
  MAX_LSN = 20
  MAX_PAGES = 10
  IndexTypes = {btree, hash, art, lsm, fractal, bloom, skip, ttree, radix}

INVARIANTS
  TypeOK_IndexSubsystem
  Inv_IndexSubsystem_Consistency

CONSTRAINT
  Cardinality(DOMAIN indexRegistry) <= 10

