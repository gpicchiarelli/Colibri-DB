═══════════════════════════════════════════════════════════════════════════
  COLIBRI-DB UPGRADE RUNBOOK
═══════════════════════════════════════════════════════════════════════════

PURPOSE: Upgrade ColibrìDB to new version safely

UPGRADE PATH: v0.8.0 → v0.9.0 → v1.0.0

───────────────────────────────────────────────────────────────────────────
1. PRE-UPGRADE CHECKLIST
───────────────────────────────────────────────────────────────────────────

□ Current version:  $ coldb --version
□ Backup complete:  $ coldb backup --output /backup/colibri.$(date +%Y%m%d).tar.gz
□ Backup verified:  $ tar -tzf /backup/colibri.*.tar.gz | wc -l  (>0 files)
□ Disk space OK:    $ df -h /var/lib/colibri-db/  (>20GB free)
□ No active users:  $ coldb sql "SELECT count(*) FROM pg_stat_activity;"  (=0)

───────────────────────────────────────────────────────────────────────────
2. STOP CURRENT VERSION
───────────────────────────────────────────────────────────────────────────

$ coldb shutdown --graceful --timeout 60

# Wait for: "Server stopped ✓"

$ ps aux | grep coldb-server
# Must show: No process running

───────────────────────────────────────────────────────────────────────────
3. INSTALL NEW VERSION
───────────────────────────────────────────────────────────────────────────

$ curl -L https://releases.colibri-db.org/v1.0.0/colibri-db-macos-arm64.tar.gz -o colibri.tar.gz

$ tar -xzf colibri.tar.gz

$ sudo mv coldb* /usr/local/bin/

$ coldb --version
# Must show: ColibrìDB 1.0.0

───────────────────────────────────────────────────────────────────────────
4. RUN FORMAT MIGRATION
───────────────────────────────────────────────────────────────────────────

$ coldb migrate \
    --from 0.9.0 \
    --to 1.0.0 \
    --file /var/lib/colibri-db/data/main.db \
    --verify

# Expected output:
#   📦 ColibrìDB Format Migration Tool
#   ✓ Loaded file (1234567 bytes)
#   🔄 Migrating...
#   ✓ Migration complete (1234600 bytes)
#   🔍 Verifying migration integrity...
#   ✓ Migration verified: data integrity preserved
#   ✓ Written to: main.db.migrated
#   ✅ Migration successful!

───────────────────────────────────────────────────────────────────────────
5. BACKUP OLD FORMAT (SAFETY)
───────────────────────────────────────────────────────────────────────────

$ mv /var/lib/colibri-db/data/main.db /var/lib/colibri-db/data/main.db.v0.9.0

$ mv /var/lib/colibri-db/data/main.db.migrated /var/lib/colibri-db/data/main.db

───────────────────────────────────────────────────────────────────────────
6. START NEW VERSION
───────────────────────────────────────────────────────────────────────────

$ coldb-server --config colibridb.conf.json

# Monitor logs for: "Server ready ✓"

───────────────────────────────────────────────────────────────────────────
7. POST-UPGRADE VERIFICATION
───────────────────────────────────────────────────────────────────────────

$ coldb health
# Must show: All OK ✓

$ coldb sql "SELECT version();"
# Must show: ColibrìDB 1.0.0

$ coldb sql "SELECT COUNT(*) FROM pg_tables;"
# Compare: Same table count as before upgrade

───────────────────────────────────────────────────────────────────────────
8. ROLLBACK (IF UPGRADE FAILS)
───────────────────────────────────────────────────────────────────────────

$ coldb-server stop

$ mv /var/lib/colibri-db/data/main.db /var/lib/colibri-db/data/main.db.failed

$ mv /var/lib/colibri-db/data/main.db.v0.9.0 /var/lib/colibri-db/data/main.db

# Reinstall old version:
$ sudo mv /backup/coldb-v0.9.0/* /usr/local/bin/

$ coldb-server --config colibridb.conf.json

───────────────────────────────────────────────────────────────────────────
9. CLEANUP (AFTER SUCCESSFUL UPGRADE)
───────────────────────────────────────────────────────────────────────────

# Keep old format for 30 days, then:
$ rm /var/lib/colibri-db/data/main.db.v0.9.0

───────────────────────────────────────────────────────────────────────────
10. NOTES
───────────────────────────────────────────────────────────────────────────

- Always test upgrade in staging first
- Upgrades should be done during maintenance window
- Keep old version binary for 30 days for emergency rollback

═══════════════════════════════════════════════════════════════════════════

